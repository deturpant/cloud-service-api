---
server:
  port: 8080
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/cloud-service
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update